import styled from "styled-components";
import { BaseProfileImage } from "./BaseProfileImage";

export const HeaderProfileImage = styled(BaseProfileImage)`
    max-width: 5rem;
    max-height: 5rem;
`;