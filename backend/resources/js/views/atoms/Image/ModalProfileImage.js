import styled from "styled-components";
import { BaseProfileImage } from "./BaseProfileImage";

const ModalProfileImage = styled(BaseProfileImage)`
    max-width: 16rem;
    max-height: 16rem;
`;
export default ModalProfileImage;